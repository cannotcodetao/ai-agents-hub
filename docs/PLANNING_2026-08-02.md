# 规划方案 · 2026-08-02（已执行）

> 提交人：WorkBuddy
> 状态：**已执行 2026-08-02（A/B/B-4/C/D/E/F-2 全部落地，校验 PASS + build 通过，已随 `c3776a9` 推送 origin/main）**
> 关联项目：`D:\AI\project\IdeaCreate\ai-agents-hub`（品牌「AI 工具策展」）
> 关联文档：`docs/TUTORIALS_TASKS_2026-08-02.md`（执行 prompt 见末尾）
> 修订 2026-08-02：① Trae IDE / WorkBuddy 介绍改为特征化 1-2 句（见任务 B-4）；② 原任务 F（badge 简化）取消，改为 **任务 F-2 · 更新执行 prompt 与项目进度文件**。

---

## 背景

用户在 2026-08-02 15:02 提交 7 条「使用教程」页 + 侧栏导航的问题，并明确要求：
- 「把这些任务先写进项目进度等相关文档，并给出执行 prompt，因为我要在新的任务窗口下执行这些任务」
- 「如果我的指令不够清晰，则跟我确定之后再执行」

---

## 本轮任务清单（按优先级）

### 任务 A · Sidebar 快捷导航修复 [HIGH]

**问题**：在 `view === 'tutorials'` 状态下，左侧栏「GitHub 今日趋势」（`#trending-github`）和「趋势榜」（`#trending`）按钮无效——这两个 `<a href="#xxx">` 是锚点跳转，而这两个 anchor ID 只存在于首页的 `TrendingGitHub` / `Trending` 组件中。

**现状代码**：
- `src/components/Sidebar.tsx:101-116` — 两个 `<a href="#trending-github">` 和 `<a href="#trending">`
- `src/App.tsx:30` — `const [view, setView] = useState<'home' | 'tutorials'>('home');`

**解决方案**：
- Sidebar 接收 `currentView: 'home' | 'tutorials'` + `onNavigateHome: (anchor: string) => void` props
- 当 `currentView === 'tutorials'` 时，点击这两个按钮：
  1. 调用 `setView('home')` 切回首页
  2. 等待视图切换完成（用 `requestAnimationFrame` 或 `setTimeout(0)`）
  3. `scrollIntoView({ behavior: 'smooth', block: 'start' })` 到目标 anchor
- App.tsx 把 view state 和切换逻辑透传给 Sidebar
- 「入门教程」按钮保持不变（`onTutorials` 切到 tutorials 视图）

### 任务 B · 删除教程页无关用户话语 [MEDIUM]

**问题**：教程页多处出现"邀请奖励 / 平台返利"等营销话术，对实际用户使用无帮助。

**待删除文本**：
1. `Tutorials.tsx:88` H2 文案：「用邀请链接，双方得奖励」
2. `Tutorials.tsx:89` 段落：「通过下方邀请链接注册 / 下载，你和作者都能获得平台奖励。」
3. `Tutorials.tsx:161` 段落：「以下平台支持「邀请新用户送奖励」，注册即可获得免费 Tokens。」

**替换文本**：
- 任务 B-1：H2 改为「下载工具」，section-label 保留「工具下载」
- 任务 B-2：删除 H2 下方的说明段落（直接进卡片网格）
- 任务 B-3：H2 改为「免费 Token 与 API 配置」，段落改为「下面 9 个平台提供免费 API 额度，注册即可使用」
- 任务 B-4（新增）· Trae IDE / WorkBuddy 介绍改为特征化 1-2 句：当前 `Tutorials.tsx` TOOLS 数组的 `desc` 偏短、像标签。改为 1~2 句、突出产品特点的介绍。建议文案：
  - `Trae IDE`：「字节跳动推出的 AI 原生集成开发环境（IDE），内置可自主完成编码、重构与调试的 SOLO 智能体，支持在「传统 IDE 模式」与「自然语言对话模式」之间切换，让 AI 直接替你写代码。」
  - `WorkBuddy`：「WorkBuddy 是一款覆盖桌面端 / 网页版 / 移动端的 AI Agent 办公助手，用自然语言即可驱动它交付研究报告、编写代码、自动化日常任务，并支持接入你自己的大模型 API Key 解锁更强能力。」
  - 执行时直接替换 `desc` 字段；允许按语气微调，但必须保持 1~2 句、且体现「谁出的 / 干什么 / 适合谁」。

### 任务 C · 卡片右上角 Download 图标 + 官网链接 [MEDIUM]

**问题**：当前 Trae IDE / WorkBuddy 卡片右上角的 `<Download />` 图标是装饰性元素，没有点击行为；旁边还有「官网 ↗」链接，与「用我的邀请链接」按钮功能重叠。

**用户原话**：「卡片右上角的下载图标应该配合下载跳转，或者直接移动至"用我的邀请链接"旁边。删掉官网下载链接，只保留我的邀请链接下载。」

**解决方案**（采用用户给的两个选项中的**第二个**——移至按钮旁边）：
1. 删除卡片右上角的孤立 `<Download />` 图标（line 99）
2. 删除「官网 ↗」链接整段（line 112-120）
3. 在「下载链接」按钮内左侧加 `<Download />` 图标（替换原来的 `<Gift />`，更贴合"下载"语义）
4. 卡片其他部分保持不变

### 任务 D · 按钮文案重命名 [LOW]

**问题**：「用我的邀请链接」语义模糊，让用户误以为要"注册链接"才能下载。

**修改**：
- `Tutorials.tsx:110` 按钮文本：「用我的邀请链接」→「下载链接」
- 同时更新 `Tutorials.tsx:7` 注释：「工具下载：用你的邀请链接（双方得奖励）」→「工具下载：用你的邀请链接直接下载（按钮显示为"下载链接"）」
- 图标从 `<Gift />` 改为 `<Download />`（配合任务 C）

### 任务 E · Token 平台卡片文案改为平台介绍 [MEDIUM]

**问题**：当前 9 个平台卡片的 `note` 字段**只讲"邀请新用户奖励"政策**，未介绍平台本身的定位、模型、特色。教程页的目的是让用户**了解并选择合适平台**，而不是推广邀请链接。

**修改策略**：每张卡片用 2~3 行简介介绍平台**做什么 / 提供哪些模型 / 适合谁**，把"邀请奖励"信息降级为角标或备注行。

**目标内容方向**（待执行 prompt 里研究/补充）：
- 硅基流动 → 国产开源模型 API 聚合 + 高性价比 + 满血 DeepSeek/Qwen
- 七牛云 AI 推理 → 云厂商级 SLA + 多模型 API + 适合企业接入
- 智谱 AI → 清华 GLM 系列 + 国产顶配 + ChatGLM API
- DeepSeek → 国产推理性价比王 + V3/R1 + 长上下文
- MiniMax → 多模态强项 + 视频/语音生成 + Hailuo
- PPIO 派欧云 → 分布式推理 + 模型托管 + 适合私有化
- 科大讯飞星火 → 中文 NLP 老牌 + 语音识别 TTS 强 + 政企合规
- 快手 StreamLake → 快手系 AI + KAT-Coder 代码模型 + 视频理解
- 天翼 AI（中国电信）→ 国资云 + 安全合规 + 政企首选

### 任务 F-2 · 更新执行 prompt 与项目进度文件 [LOW]

**问题**：本轮交付物是「给新窗口的执行 prompt」，但规划会随执行演化（如本次把 F 改为 F-2、补充 Trae/WorkBuddy 特征化介绍）。若不回写文档，规划与进度会失真，下次续接时看不懂。

**修改（新窗口收尾时做）**：
- `docs/PROGRESS.md`：追加 2026-08-02 新窗口执行子条目，标记 A~E + F-2 共 6 任务完成
- `docs/TASK_PLAN.md`：加 Phase 11（教程页 6 任务）并标记 complete
- 本文档（PLANNING_2026-08-02.md）顶部状态从「已修订 2026-08-02」改为「已执行」
- `docs/TUTORIALS_TASKS_2026-08-02.md` 顶部决策行 F-2 标记「已执行」
- 如执行中发现偏差（如某平台介绍需改），同步更新对应 prompt 内容，保持文档即真相

**说明**：原任务 F（badge 简化）已取消，不再改造 badge；其文档同步职责并入 F-2。

---

## 决策清单（待用户勾选）

- **A · 侧栏导航** ✅ 教程页点击锚点 → 切回首页 + 平滑滚动到 anchor
- **B · 删除营销话术** ✅ 删除 3 处，邀请链接依然保留（按钮文本改为"下载链接"）
- **C · Download 图标 + 官网链接** ✅ 删除右上角孤立图标 + 删除官网链接 + 按钮内图标改为 Download
- **D · 按钮文案** ✅ 「用我的邀请链接」→「下载链接」
- **E · Token 平台文案** ✅ 全部 9 个平台从「邀请奖励」改为「平台介绍」
- **F（已改为 F-2）· 更新执行 prompt 与项目进度文件** ✅ 取消 badge 改造，改为收尾回写文档

> 原 F（badge 简化）已取消，并入 F-2 的文档同步职责。A~E + F-2 共 6 项，均已明确。

---

## 风险/边界

1. **任务 A 的视图切换 + 滚动时序**：需要确保 DOM 渲染完成后再 scroll。用 `requestAnimationFrame` + `setTimeout(50)` 双保险。
2. **任务 E 平台介绍准确性**：每条需简短（30~60 字）且客观，不夸大、不贬低；价格/额度信息不再展示（让用户到官网自行查）。
3. **任务 B 删除文案后空白**：删除段落会导致 H2 下空荡一坨，需视觉上保留呼吸感——section-label 已够用，段落可直接删。
4. **任务 F badge 改动不影响校验脚本**：check_caps_dup.py 不校验 badge 文案，可安全改。

---

## 关联执行 Prompt

完整执行 prompt 见同目录 `docs/TUTORIALS_TASKS_2026-08-02.md`。**这是给你复制到新窗口的内容**，不是当前会话执行。